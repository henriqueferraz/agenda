/**
 * Configuração do PostCSS: usa o plugin @tailwindcss/postcss para processar classes do Tailwind.
 */

const config = {
  plugins: {
    // Plugin Tailwind CSS para processamento de classes utilitárias
    "@tailwindcss/postcss": {},
  },
};

export default config;
